{
  "name": "${project}",
  "version": "1.0.0",
  "description": "web app using epii",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development node index.js",
    "prod": "node index.js",
    "make": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "${author}",
  "license": "MIT",
  "dependencies": {
    "epii-server": "^0.3.1",
    "whatwg-fetch": "^2.0.3"
  },
  "devDependencies": {
    "epii-render": "^0.6.0"
  }
}
